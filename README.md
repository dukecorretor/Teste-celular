# Teste-celular

Página romântica gerada automaticamente — abra `index.html` no seu navegador.

Instruções rápidas:

- Abra o arquivo [index.html](index.html) no navegador.
- Ou, no terminal do container, execute:

```bash
$BROWSER index.html
```

- Para personalizar a mensagem, edite `index.html` (elemento `<p class="lead">`).

Feito com carinho 💖